# Keycloak Configuration
keycloak:
  image:
    repository: acr9tfiqx.azurecr.io/keycloak-custom
    tag: d2e68de8755f0a814404c7f88c5bf46779823d6a
    pullPolicy: IfNotPresent
  
  service:
    type: LoadBalancer
    port: 8080
    targetPort: 8080
  
  replicas: 1
  
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"

# PostgreSQL Configuration
postgres:
  enabled: true
  image: postgres:15
  port: 5432
  username: keycloak
  password: password123  # POC only - should use Secrets in production
  database: keycloak
  
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "250m"

# Database connection settings
database:
  vendor: postgres
  port: 5432
  host: postgres-service  # Service name for PostgreSQL
